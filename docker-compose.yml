services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: chulla
      POSTGRES_PASSWORD: chulla
      POSTGRES_DB: chulla
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
