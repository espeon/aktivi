services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: aktivi
      POSTGRES_PASSWORD: aktivi
      POSTGRES_DB: aktivi
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
